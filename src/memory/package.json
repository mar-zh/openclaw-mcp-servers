{
  "name": "@openclaw/memory-server",
  "version": "1.0.0",
  "description": "OpenClaw mesh-aware MCP memory server with temporal observations and enhanced search",
  "license": "SEE LICENSE IN LICENSE",
  "mcpName": "openclaw/memory-server",
  "author": "OpenClaw",
  "type": "module",
  "bin": {
    "mcp-server-memory": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@vitest/coverage-v8": "^2.1.8",
    "shx": "^0.3.4",
    "typescript": "^5.6.2",
    "vitest": "^2.1.8"
  }
}